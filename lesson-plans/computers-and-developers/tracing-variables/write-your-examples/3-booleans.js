'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

let x = 5; // Declare and initialize x as 5.
let y = 6; // Declare and initialize y as 6.

let z = 'True'; // Declare and initialize z as 'True'.
let a = 'False'; // Declare and initialize a as 'False'.

if (x == y) {
  // x and y are same value or not.
  console.log(z); // Read z, condition not applied
} else {
  console.log(a); // Read a, condition applied. Call log.
}
